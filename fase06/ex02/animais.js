function animais(array){
    var obj = array;
    var novalidade = 5;
    var i = 0;
    
    while (i< array.length) {
        var posicao = obj [i];
        delete podicao.altura
        if (posicao.idade !== null) {
            posicao.idade = novaIdade}
            novaIdade++;
    }
    return array
}

